# Claude Instructions (TypeScript Skill)

## Source of truth

- **Follow `{{PRINCIPLES_PATH}}` as the single source of truth.**
- If a rule conflicts with explicit project requirements, project requirements win.
- If anything in this file conflicts with `{{PRINCIPLES_PATH}}`, **`{{PRINCIPLES_PATH}}` wins**.
- Keep `AGENTS.md`, `CLAUDE.md`, `CURSOR.md`, `.github/copilot-instructions.md`, and `rulebook.yaml` aligned with `{{PRINCIPLES_PATH}}`.

## Quick guardrails (summary)

This is a non-exhaustive summary. Always prefer the absolute mapping definitions outlined in [TypeScript Coding Principles]({{ROUTER_PATH}}) and the detailed rule documents in `{{RULES_PATH}}/`.
