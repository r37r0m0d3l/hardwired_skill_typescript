# Agent Instructions (TypeScript Skill)

## Source of truth

- **Follow `principles.md` as the single source of truth.**
- If a rule conflicts with explicit project requirements, project requirements win.
- If anything in this file conflicts with `principles.md`, **`principles.md` wins**.
- Keep `AGENTS.md`, `CLAUDE.md`, `CURSOR.md`, `.github/copilot-instructions.md`, and `rulebook.yaml` aligned with `principles.md`.

## Using the skill rules

- **Read the core principles first** — it acts as the table of contents for the core rules:
	- [TypeScript Coding Principles]({{ENCODED_PUBLIC_ROUTER_PATH}})
- When applying TypeScript guidelines, consult the relevant rule document for context and best practices.

## Quick guardrails (summary)

This is a non-exhaustive summary. Always prefer the absolute mapping definitions outlined in [TypeScript Coding Principles]({{ENCODED_PUBLIC_ROUTER_PATH}}).
