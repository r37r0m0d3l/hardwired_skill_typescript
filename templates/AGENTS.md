# Agent Instructions (TypeScript Skill)

## Source of truth

- **Follow `{{PRINCIPLES_PATH}}` as the single source of truth.**
- If a rule conflicts with explicit project requirements, project requirements win.
- If anything in this file conflicts with `{{PRINCIPLES_PATH}}`, **`{{PRINCIPLES_PATH}}` wins**.
- Keep `AGENTS.md`, `CLAUDE.md`, `CURSOR.md`, `.github/copilot-instructions.md`, and `rulebook.yaml` aligned with `{{PRINCIPLES_PATH}}`.

## Using the skill rules

- **Read the core principles first** - it acts as the table of contents for the core rules:
	- [TypeScript Coding Principles]({{ROUTER_PATH}})
- When applying TypeScript guidelines, consult the relevant rule document for context and best practices.
- For specific implementations, consult the relevant rule files located inside `{{RULES_PATH}}/`.

## Quick guardrails (summary)

This is a non-exhaustive summary. Always prefer the absolute mapping definitions outlined in [TypeScript Coding Principles]({{ROUTER_PATH}}).
