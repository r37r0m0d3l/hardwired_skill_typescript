# Agent Instructions (TypeScript Skill)

## Source of truth

- **Follow `principles.md` as the single source of truth.**
- If a rule conflicts with explicit project requirements, project requirements win.
- If anything in this file conflicts with `principles.md`, **`principles.md` wins**.
- Keep `AGENTS.md`, `CLAUDE.md`, `.github/copilot-instructions.md`, and `rulebook.yaml` aligned with `principles.md`.

## Using the skill rules

- **Read `principles.md` first** — it acts as the table of contents for the core rules.
- **Each rule is documented in `./rules/`** — refer to the relevant rule file for detailed explanations and examples.
- When applying TypeScript guidelines, consult the relevant rule document for context and best practices.

## Quick guardrails (summary)

This is a non-exhaustive summary. Always prefer `principles.md` and the detailed rule documents in `./rules/`.
